
import { _decorator, Component, screen, Graphics, view } from 'cc';
const { ccclass, property } = _decorator;

@ccclass('Debug')
export class Debug extends Component {
    start() {
        // const { windowSize } = screen;
        // // const windowSize = view.getVisibleSizeInPixel();
        // console.log(windowSize);
        // const g = this.node.getComponent(Graphics);
        // g.moveTo(0, 0);
        // g.lineTo(windowSize.width >> 1, 0);
        // g.lineTo(windowSize.width >> 1, windowSize.height >> 1);
        // g.lineTo(0, windowSize.height >> 1);
        // g.lineTo(0, 0);
        // g.fill();

    }
}